# @creador-dev/utils

Import from the package root or a focused entry point:

```ts
import { clamp } from "@creador-dev/utils/clamp";
import { cn } from "@creador-dev/utils/cn";
```

Shared dependency-free utilities. Import named exports from `@creador-dev/utils`.
