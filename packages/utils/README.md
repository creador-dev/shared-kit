# @wpxdev/utils

Import from the package root or a focused entry point:

```ts
import { clamp } from "@wpxdev/utils/clamp";
import { cn } from "@wpxdev/utils/cn";
```

Shared dependency-free utilities. Import named exports from `@wpxdev/utils`.
